# Local .terraform directories
**/.terraform/*

# .tfstate filing
*.tfstate
*.tfstate.*

# Crash log filing
crash.log
crash.*.log

# Exclude all .tfvars filing, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override filing as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override filing you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan filing to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration filing
.terraformrc
terraform.rc